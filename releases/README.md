# Releases

- `manifests/` — manifeste par release (versions, checksums, composants, flags actifs)
- `changelogs/`, `release-notes/` — notes générées depuis les change sets
- `deployment/` — enregistrements de déploiement par environnement
- `rollback/` — points de restauration (version précédente, snapshot config, état flags)
