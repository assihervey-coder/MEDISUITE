name: Build image
on:
  push: {tags: ["v*"]}
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build (sans push — registry à configurer)
        run: docker build -t tropirag:${{ github.ref_name }} .
