numpy>=1.26
pyyaml>=6.0
# v0.2/v0.3 — backends optionnels (ADR 0022 encodeurs, ADR 0023 fusion entraînable)
# torch>=2.2   (CPU : pip install torch --index-url https://download.pytorch.org/whl/cpu)
#   requis pour fusion_model_* (TorchFusionModel entraînable) et backend torch/monai
# monai>=1.3   (prétraitement imagerie ; exige torch compatible)
