Générateur de diff : `python -m evolution-control-plane.engines.impact-engine.engine`
+ `application/analysis/analyze_impact.py --diff-only`.
Entrée : baseline + change set. Sortie : rapport YAML signé dans ../reports/.
